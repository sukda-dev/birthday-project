import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://sukda-dev.github.io/birthday-project", // URL เว็บไซต์จริง
  base: "/birthday-project/", // ใช้ชื่อ repo ของคุณ
});